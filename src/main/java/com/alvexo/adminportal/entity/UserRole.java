package com.alvexo.adminportal.entity;

/** Mirrors vehicle-service-booking-api's UserRole enum — same `users.role` column values. */
public enum UserRole {
    VEHICLE_USER,
    MECHANIC,
    SALES_REPRESENTATIVE,
    ADMINISTRATOR
}
