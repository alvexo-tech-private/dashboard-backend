package com.alvexo.adminportal.entity;

/** The 3 fixed admin-portal roles (ADMIN-US-29) — mirrors dashboard-frontend's AdminRole enum. */
public enum AdminRole {
    SUPER_ADMIN,
    FINANCE_ADMIN,
    OPERATIONS_ADMIN
}
